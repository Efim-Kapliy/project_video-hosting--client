export function SidebarSubscription() {
	return (
		<nav>
			<ul>
				{/* {menu.map(menuItem => (
					<SubItem
						key={menuItem.label}
						item={menuItem}
					/>
				))} */}
			</ul>
		</nav>
	)
}
